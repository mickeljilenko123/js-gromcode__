const arr1 = [2, 3, 5, 6];
const num1 = 6;

const includes = (arr, num) => {
    if (arr1.includes(num1)) {
        return true;
    } else {
        return false;
    }
}
console.log(includes(arr1, num1));